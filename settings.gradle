def modules = [
        'gilli-pre',
        'gilli-core',
        'gilli-science-conversion',
        'gilli-cover'
]

modules.each { mod ->

    include ":$mod"
    project(":$mod").projectDir = new File(settingsDir, "modules/$mod")
}

